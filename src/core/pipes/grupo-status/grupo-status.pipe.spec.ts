import { GrupoStatusPipe } from './grupo-status.pipe';

describe('GrupoStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new GrupoStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
