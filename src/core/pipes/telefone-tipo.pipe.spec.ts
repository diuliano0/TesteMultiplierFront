import { TelefoneTipoPipe } from './telefone-tipo.pipe';

describe('TelefoneTipoPipe', () => {
  it('create an instance', () => {
    const pipe = new TelefoneTipoPipe();
    expect(pipe).toBeTruthy();
  });
});
